#pragma once
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int StringToInt(const char[]);
char* scanString(char*);
int intLen(int);
//char* build_sql_request(char* buffer );

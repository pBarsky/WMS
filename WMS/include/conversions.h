#pragma once
#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int stringToInt(const char*);
int intLen(int);
int scanInt();
int hashPassword(const char*);
char *scanString();

#pragma once
#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int string_to_int(const char*);
int int_len(int);
int scan_int();
int hash_password(const char*);
char* scan_string();
